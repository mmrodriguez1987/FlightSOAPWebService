<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="
           http://maven.apache.org/POM/4.0.0
           https://maven.apache.org/xsd/maven-4.0.0.xsd">

  <modelVersion>4.0.0</modelVersion>
  <groupId>com.example</groupId>
  <artifactId>FlightServiceSOAP</artifactId>
  <version>1.0-SNAPSHOT</version>
  <packaging>war</packaging>

  <dependencies>
  <!-- JAX-WS Reference Implementation (runtime) -->
  <dependency>
    <groupId>com.sun.xml.ws</groupId>
    <artifactId>jaxws-rt</artifactId>
    <version>2.3.4</version>
  </dependency>

  <!-- SAAJ API (JAXB‑WS needs this on Java 11+) -->
  <dependency>
    <groupId>javax.xml.soap</groupId>
    <artifactId>javax.xml.soap-api</artifactId>
    <version>1.4.0</version>
  </dependency>

  <!-- SAAJ Implementation (provides com.sun.xml.internal.messaging.saaj.*) -->
  <dependency>
    <groupId>com.sun.xml.messaging.saaj</groupId>
    <artifactId>saaj-impl</artifactId>
    <version>1.5.3</version>
  </dependency>

  <!-- (any other existing dependencies…) -->
</dependencies>


  <build>
  <plugins>
    <plugin>
      <groupId>org.codehaus.mojo</groupId>
      <artifactId>jaxb2-maven-plugin</artifactId>
      <version>2.5.0</version>
      <executions>
        <execution>
          <id>xjc-generate</id>
          <goals>
            <goal>xjc</goal>
          </goals>
          <configuration>
            <packageName>com.example.flightservicesoap</packageName>           
            <schemaDirectory>${project.basedir}/src/main/resources</schemaDirectory>          
            <schemaIncludes>
                <include>flight.xsd</include>
            </schemaIncludes>            
            <sources>
                <source>${project.basedir}/src/main/resources</source>
            </sources>
          </configuration>
        </execution>
      </executions>
    </plugin>

    <!-- your existing compiler plugin, etc. -->
    <plugin>
      <groupId>org.apache.maven.plugins</groupId>
      <artifactId>maven-compiler-plugin</artifactId>
      <version>3.10.1</version>
      <configuration>
        <source>1.8</source>
        <target>1.8</target>
      </configuration>
    </plugin>
    
    <plugin>
        <groupId>org.codehaus.mojo</groupId>
        <artifactId>exec-maven-plugin</artifactId>
        <version>3.1.0</version>
        <configuration>
            <!-- Fully‑qualified name of your Publisher class -->
            <mainClass>com.example.flight.service.Publisher</mainClass>
            <!-- (Optional) Ensure classpathScope is compile, so generated JAXB classes are included -->
            <classpathScope>compile</classpathScope>            
            <!-- OPEN the jdk.internal.misc package so Metro can use Unsafe via reflection -->
        <jvmArguments>--add-opens=java.base/jdk.internal.misc=ALL-UNNAMED</jvmArguments>
        </configuration>
        <!-- You can even bind it to a phase, but not strictly necessary -->
        <!--
            <executions>
              <execution>
                <goals><goal>java</goal></goals>
              </execution>
            </executions>
            -->
</plugin>

  </plugins>
</build>
</project>
